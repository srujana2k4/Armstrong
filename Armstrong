#include<stdio.h>
#include<conio.h>
void main()
{
	int num,org,rem,sum=0;
	printf("Enter a number");
	scanf("%d",&num);
	org=num;
	while(org!=0)
	{
		rem=org%10;
		sum=sum+rem*rem*rem;
		org=org/10;
	}
	if(sum==num)
	printf("%d-Armstrong",num);
	else
	printf("%d-Not Armstrong",num);
getch();
}
